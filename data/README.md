# Data directory

- `raw/`: immutable source data.
- `processed/`: cleaned or transformed datasets used by analysis scripts.

Legacy input files remain under `Analyses/inputs/` until migration is completed.
